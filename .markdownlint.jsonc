{
  // MD013(line-length, 기본 80칼럼)은 이 저장소에 맞지 않아 끈다.
  //  - 문서 대부분이 한국어 산문이다. 린터는 한글 1자를 1칼럼으로 세지만 실제로는 2칼럼
  //    폭으로 렌더링되므로 칼럼 수 자체가 의미를 갖지 못한다.
  //  - 와이어 포맷 ASCII 다이어그램과 hex 덤프는 줄을 접으면 그림이 깨진다.
  //  - 원본 SRS 대조표처럼 열이 많은 표는 애초에 줄바꿈이 불가능하다.
  "MD013": false
}
